let array = [object = {x: 5, y: 10}]

function detect() {
	otherObj = {
   x: array[0].x,
 	 y: array[0].y
    }
    return(otherObj)
	
	
	
}
console.log(detect().x);


if (this.alive && this.state == "hungry") {
    
  }